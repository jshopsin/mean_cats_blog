<h1>Categories</h1>

<article>
  <% if @categories %>
    <% @categories.each do |category| %>
      <h3><%= link_to category.name, category_path(category) %></h3>
    <% end %>
  <% end %>
</article>